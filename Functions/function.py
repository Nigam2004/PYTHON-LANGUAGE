# Syntax->def fun_name(parameters):
#     fun_body

def add(a,b):
   return(a+b)

x=add(1,2)
print(x)


"""
explanation:
  On the above functin a,b are the  parameters and 1,2 are the argumenet
 a,b=parameters(parameter is act like a container)
 1,2= argument (argument are the real value which you input)
 return= return the value/which user want to return to the function

 Funa

 """